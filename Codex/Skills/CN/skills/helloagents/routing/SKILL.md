---
name: routing
description: 路由机制详细规则；复杂边界路由判定、命令确认、上下文响应或开发实施入口判断时读取。详细决策表按需读取 references。
invocation: model
side_effects: read-only
requires:
  - output-format
completion_criteria: 已完成意图、授权、风险、调试信号和 EHRB 判定，并给出唯一处理路径或明确追问。
---

# 路由机制

**目标:** 将用户消息映射到咨询问答、命令路径、系统化调试、开发模式或上下文响应。主文件保留最小决策流，详细规则按需读取。

---

## 快速决策流

1. 扫描命令词：`~auto` / `~helloauto` / `~fa` / `~plan` / `~design` / `~exec` / `~run` / `~execute` / `~test` / `~init` / `~wiki`。
2. 判断是否为上一阶段的确认、选择或反馈。
3. 判断是否存在 Bug、测试失败、构建失败、运行异常、性能回退或 flaky 信号。
4. 判断用户是否明确授权改动，以及请求是只读还是写入型。
5. 按可逆性、外部副作用、公共契约、数据迁移、不确定性和 EHRB 判定低/中/高风险。
6. 选择成本与风险相称的路径；高风险或关键不确定性无法规避时暂停确认。

---

## Reference 选择

| 需要解决的问题 | 读取文件 |
|---|---|
| 评分、路由优先级、模式判定 | `references/routing-decision.md` |
| 微调/轻量/标准/完整研发、调试路径和开发实施硬门禁 | `references/routing-paths.md` |
| 命令确认、规划命令、上下文响应 | `references/commands-and-context.md` |

---

## 正反例

**应触发:**
- 用户输入 `~plan`、`~exec` 或确认方案继续。
- 用户报告“测试失败”“构建失败”“线上接口异常”。
- 用户请求跨文件改动，但范围或风险边界不清。

**不应触发:**
- 简单事实问答且不需要阶段流。
- 已在明确开发实施中执行具体文件编辑时重复路由。

---

## 完成门禁

- 意图类型已判定：问答型、改动型或命令型。
- 改动授权和影响范围已判定。
- 风险等级、调试信号和 EHRB 信号已显式判定。
- 最终路由唯一；若不唯一，按 G3 输出不确定因素和选项。
