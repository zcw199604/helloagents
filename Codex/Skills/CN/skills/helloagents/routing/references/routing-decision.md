# 路由决策规则

## 路由流程

对每条用户消息执行以下步骤：

1. **状态检查**: 读取 `WORKFLOW_STATE` 与 `PENDING_INTERACTION`；等待态回复仅消费一次，明确新需求先结束旧等待态再重新路由
2. **信息提取**: 扫描命令词、上下文状态、意图、EHRB信号
3. **路由决策**: 按路由优先级匹配

---

## 路由优先级

**互斥决策树（按序匹配，命中即停止）:**
```yaml
1. 命令模式（~auto/~plan/~exec/~test/~init）
2. 上下文响应（追问/选择/确认/反馈）
3. 系统化调试模式（Bug/测试失败/构建失败/运行异常/性能回退/flaky）
4. 开发模式（微调→轻量迭代→标准开发→完整研发）
5. 咨询问答（兜底）
```

---

## 评估维度

```yaml
主要维度:
  意图类型: 问答型 | 改动型 | 命令型
  改动范围: 无 | 微(≤2文件≤30行) | 小(3-5文件) | 中(多文件) | 大(架构级) | 不确定
  需求明确度: 明确 | 模糊 | 需澄清
  上下文状态: 无 | 追问中 | 选择中 | 确认中
  命令修饰: 无 | ~auto | ~plan | ~init | ~exec | ~test

辅助维度:
  调试信号: Bug | 测试失败 | 构建失败 | 运行异常 | 性能回退 | flaky | 无
  EHRB风险信号: 有 | 无
  TDD适用性: 强制 | 建议 | 例外 | 不确定
  关键词: prod|production|live|DROP|TRUNCATE|rm -rf|密钥|支付|bug|报错|异常|失败|回归|flaky|timeout|stack trace|污染|polluter|单跑通过|整套失败|边界|链路
```

---

## 决策原则

- 微调/轻量迭代/标准开发 条件为"全部满足"型，任一不满足则降级
- 完整研发 条件为"满足任一"型，作为保守兜底
- 不确定时默认 完整研发

---

## 路由验证

<routing_verification>

⚠️ **CRITICAL - 强制执行规则:**

**路由前验证（在 <thinking> 中完成）:**
1. **意图类型**: [问答型/改动型/命令型] - 依据: [用户原话引用]
2. **改动范围**: [无/微/小/中/大/不确定] - 依据: [文件数/行数估算]
3. **调试信号**: [Bug/测试失败/构建失败/运行异常/性能回退/flaky/无] - 依据: [错误/日志/关键词]
4. **EHRB信号**: [有/无] - 依据: [关键词扫描结果]
5. **最终路由**: [咨询问答/系统化调试/微调/轻量迭代/标准开发/完整研发]

**路由后重述（在输出中）:**
- 如果路由到开发模式（非咨询问答）: "已判定为[模式名称]，原因: [1-2句解释]"
- 如果路由到系统化调试模式: "已判定为系统化调试模式，原因: 检测到[调试信号]，将先复现并定位根因再修改"
- 如果不确定: "需求复杂度不确定，默认采用完整研发流程以确保质量"

**不确定性处理:**
- 边界情况（如文件数恰好2个）→ 参考 G3 不确定性处理原则
- 关键信息缺失 → 保守路由（选择更完整的路径）

</routing_verification>

---

